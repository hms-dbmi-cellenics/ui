const NETWORK_RESOURCES = 'networkResources';

/**
 * Loads name environment for the current request.
 */
const NETWORK_RESOURCES_DEPLOYMENT_INFO_LOADED = `${NETWORK_RESOURCES}/deploymentInfoLoaded`;

export {
  // eslint-disable-next-line import/prefer-default-export
  NETWORK_RESOURCES_DEPLOYMENT_INFO_LOADED,
};
