const validateSpatialCountMatrix = async () => {
  // No-op: there is currently no content validation for spatial count-matrix
  // techs (e.g. Xenium). Real validation lands in a later phase.
};

export default validateSpatialCountMatrix;
