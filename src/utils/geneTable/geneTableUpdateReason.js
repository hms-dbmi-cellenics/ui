const geneTableUpdateReason = Object.freeze({
  mounted: 'mounted',
  loading: 'setToLoaded',
  loaded: 'setToLoading',
  filtered: 'filtered',
  paginated: 'paginated',
  retry: 'retry',
});

export {
  // eslint-disable-next-line import/prefer-default-export
  geneTableUpdateReason,
};
